import React from 'react';

export const Tyle = ({ color, background }) => {
  return (
    <div
      style={{
        color: color,
        backgroundColor: background,
        height: '20px',
        width: '20px',
        padding: '10px',
      }}
    >
      x
    </div>
  );
};
